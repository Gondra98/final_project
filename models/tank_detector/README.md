# Tank Detector Model

Put the shared YOLO detector weight here:

```text
models/tank_detector/best_final.engine
```

Model weight files are intentionally not committed. Share `best_final.engine` through an external artifact store, GitHub release, or direct team handoff.
