# Tank Detector Model

Put the shared YOLO detector weight here:

```text
models/tank_detector/best.pt
```

Model weight files are intentionally not committed. Share `best.pt` through an external artifact store, GitHub release, or direct team handoff.
